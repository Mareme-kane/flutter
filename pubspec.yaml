name: appliflutter1
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  cupertino_icons: ^1.0.8
  async: ^2.12.0                 # Mise à jour
  material_color_utilities: ^0.11.1 # Mise à jour

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  fake_async: ^1.3.2               # Mise à jour
  leak_tracker: ^10.0.8           # Mise à jour


flutter:
  uses-material-design: true
  assets:
    - assets/images/
